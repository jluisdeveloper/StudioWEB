const variables = () => {
  const colors = {
    blue: "#0052fe",
    black: "#000000",
    white: "#FFFFFF",
    grey: "#777777"
  }

  const fonts = {
    oswald: "'Oswald', sans-serif",
    poppins: "'Poppins', sans-serif"
  }

  return { colors, fonts }
}

export default variables 